// Set this to your backend URL when hosting frontend on Vercel and backend elsewhere.
// Leave empty for same-origin (single-server deployment, e.g. local dev).
// Example: window.PULSE_API_BASE = "https://pulse-backend.up.railway.app";
window.PULSE_API_BASE = "";
